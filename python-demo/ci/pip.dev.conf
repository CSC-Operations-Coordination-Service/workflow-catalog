# pip config for the DEV context.
# Installs/pulls resolve through the Nexus dev group (your packages + cached PyPI).
# No credentials here — auth (if any) comes from the environment, never this file.
[global]
index-url = http://127.0.0.1:2375/repository/pypi-dev-group/simple
